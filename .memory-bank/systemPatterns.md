# System Patterns

## Format
- [YYYY-MM-DD]: Decision -> Reason -> Alternative considered

## Decisions
- None yet.
- 2026-07-05T18:50:54Z | status:completed | 2026-07-05: skill-router routes are structured Route records with explicit skills/tools/workers/doc_namespaces/priority. Hook analysis uses match_routes(), collect_metadata(), route_records(), and depth decisions; skill extraction from human hint text is retired.
- 2026-07-05 | status:live | Catalog cache via mtime-signature -> Reason: catalog() is called on every UserPromptSubmit (hook path) AND by audit/bench; reading 100+ SKILL.md + parsing frontmatter per prompt is wasteful when the FS rarely changes between prompts. Signature = max mtime across skills_root + every skill dir + every SKILL.md; stat() is ~10x cheaper than read_text()+parse. Invalidation is automatic on add/remove/rename/edit. Keyed by skills_root() Path so per-test tmp_path never collides. `use_cache=False` escape hatch for audit-after-edit; `clear_catalog_cache()` for tests. -> Alternative rejected: TTL cache (time-based) would serve stale data briefly and adds a clock dep; mtime-signature is exact.
- 2026-07-05 | status:live | classify() defensive envelope -> Reason: cheap_llm.cheap_complete() is an external dep whose exact return contract (dict vs None vs raise) is not guaranteed across versions; classify() promises "never raises" and feeds the hook fail-open contract. Wrap the call in try/except + isinstance(out, dict) guard so a broken/None/raising cascade degrades to meta-tier fallback instead of AttributeError. Applies to any wrapper over an external LLM cascade.
- 2026-07-07T20:55:22Z | Decision: skill-router should expose machine-readable hand choices in metadata. Code review/ecosystem prompts route to deliberation + quality sensors, while edit-time code-fact prompts route to code-intelligence/codeq with structured JSON first for workers/scripts.
- 2026-07-07T21:19:20Z | Decision 2026-07-07: skill-router should surface machine-readable hand choices in route metadata; edit-time facts use codeq, ship-time quality uses codescan.
- 2026-07-08 | status:live | Lexical-only depth fallback -> Reason: Emitting section-level routing hints dramatically saves prompt context budget for multi-level skills. Embedding-based selection was disabled in hooks due to Ollama HTTP connection overhead/latency. A deterministic token-based Jaccard similarity fallback (augmented with stem matching) allows instant (<1ms) depth matching directly inside prompt submit hooks without calling Ollama or blocking prompt submission. -> Alternative rejected: Full embedding-based check in hooks (too slow/unstable); completely omitting depth hints (lacks progressive disclosure routing context).

- 2026-07-11T18:16:31Z | status:live | 2026-07-11: High-frequency classification owns a 256-token output budget and requires cheap_llm>=1.2; cheap-llm transports enforce the limit uniformly, while classify retains fail-open meta fallback on missing/drifted dependencies.
- 2026-07-12T17:27:47Z | status:live | 2026-07-12: skill-router gana recommender semántico (features/recommend). Prompt huérfano (sin Route regex, ~33% del catálogo = 38/115 skills) cae ahora a recommend() antes de declarar unmatched: embede prompt (Ollama embeddinggemma, 0.1s warm) + cosine vs índice en disco (~/.claude/state/skill-router/catalog.npz, build incremental por mtime) -> top-k skills con cos>=0.30. Resucita huérfanas (logging-patterns, concurrency-review, maven-dependency-audit) Y entries dropeadas por skillListingBudgetFraction=0.01. Warmup fire-and-forget en SessionStart (hooks/skill-router-warmup.sh, tras ollama-warmup). Bug fixeado: BUILD_EMBED_TIMEOUT=12s para el build del catálogo (DEPTH_EMBED_TIMEOUT 1.5s mataba cada embed con cold model -> índice vacío silencioso). CLI: skill-router recommend --prompt --top-k --floor [--json --rebuild --status]. Budget 0.01 se mantiene (recommender compensa el dropeo).
- 2026-07-13T00:55:24Z | status:live | 2026-07-13: skill-router codeq/codescan routes (~//skill-router/.../route_groups/core.py) ampliadas con patrones de INTENCIÓN (where is X defined / what calls Y / find uses of X -> codeq; review this diff / is there dead code -> codescan). Diagnóstico 'Claude Code no usa tools graduadas como Codex': config SIMÉTRICA (mismo user-prompt-pipeline.py, _CONTROLLER_CALLERS={claude,codex}, 8 paquetes graduados en PATH). Gap real: regex literal-name -> routes=0 para prompts naturales -> recommender skills-only sugería skills irrelevantes (maven-dependency-audit) en vez de codeq. Fix simétrico cross-CLI. Commit 64a9a27. Factor residual NO-config: Claude tiene LSP/Read/Grep/Agent nativos más convenientes -> hint sube probabilidad, no fuerza. Recommender a tools CLI: DEFER. swarm-auto-delegate shim: HEALTHY (cli-orchestration auto_delegate.py 511L).
- 2026-07-13T12:58:42Z | status:live | skill-router diseño confirmado 2026-07-13: regex deterministico primero, rescate semantico (recommend, npz+numpy coseno, floor 0.40) solo en prompts sin ruta (~38 skills sin regex es intencional), depth lexical-only en hook (embeddings solo opt-in), warmup SessionStart. NO usar DB vectorial externa: catalogo ~115-266 skills, indice npz en ~/.claude/state/skill-router/ es suficiente y mas simple. Indice se invalida por mtime al editar SKILL.md; rebuild incremental en proxima invocacion o via sync-codex-skill-catalog.py.
- 2026-07-13T23:50:00Z | status:live | Decision: rutas de dominio declaran FAMILIA COMPLETA de skills, no solo el primario. Reason: recommender semantico SOLO corre en prompts SIN ruta. Cuando regex de dominio matchea (spring boot, n8n), solo se emite hint + skills del metadata de esa ruta; los especialistas hermanos (spring-boot-patterns, concurrency-review, n8n-code-javascript) quedan invisibles aunque sean el skill MAS relevante. Broadening el tuple skills los expone en hint (si backticks), on_demand_skill_hints (Codex), discover, audit coverage. Restricciones: backtick en hint debe estar en tuple (sino hint_drift); skill en tuple debe existir en catalog (sino ghost); unrouted es solo informativo (no rompe gate). Broadening 2026-07-13: n8n 1->10, java 5->17, angular 3->8, browser +test-browser, git +git-master. Coverage 77->105/126.

- 2026-07-15T01:34:48Z | Decision: route only canonical resolvable skills; marketplace source trees are not runtime availability. Emit on-demand load paths before optional prose, and filter Codex tool hints against PATH plus configured MCP servers.
- 2026-07-19T01:40:05Z | status:live | Decision: PR babysit (shepherd) and single-issue implementation (implement-issue) get dedicated high-priority routes (prio 88), not only lexical/semantic rescue. Reason: unmatched rescue was ~270ms and could mis-hit issue-triage on babysit prompts; dedicated regex keeps hook ~3ms and exact skill.
